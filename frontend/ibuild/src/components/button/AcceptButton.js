import React from 'react';

const AcceptButton  = () => (
    <div style={{textAlign: "right"}}>
        <button type="submit" className="btn btn-success" style={{margin: "8px", width: "100px", fontWeight: "bold", borderRadius: "50px"}}>Salvar</button>
    </div>
);

export default AcceptButton;